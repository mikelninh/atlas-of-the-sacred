# One-time source bootstrap

The production source archive in this folder is unpacked by `.github/workflows/bootstrap-production.yml`. The workflow replaces the temporary static repository shell with the full Next.js, claim registry, journey system, editorial OS and Supabase foundation, then removes this bootstrap folder.
