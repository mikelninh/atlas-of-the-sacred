# Gate 1 kickoff

The complete production application validates locally with 22 claims, 18 sources and 22 unique claim references. The local npm registry returned HTTP 503, so the dependency-backed build is delegated to GitHub CI and the Vercel preview.

The current source capsule is a transport mechanism only. Replace it with ordinary source files before merge.
